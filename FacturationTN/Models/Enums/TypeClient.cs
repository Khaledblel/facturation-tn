namespace FacturationTN.Models.Enums;

public enum TypeClient
{
    /// <summary>Valeur par défaut non sélectionnée — échoue à la validation.</summary>
    NonDefini = 0,
    PersonnePhysique = 1,
    PersonneMorale = 2
}
